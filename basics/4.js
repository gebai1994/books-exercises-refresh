/* When in a code, there is an addition of numbers and the first
is in form of a string, JavaScript will assume the second
expression is also a string and that is why it will concatenante
both expressions as 2 strings. */