//This function will separate each piece of the string that is
//separated by a space ' ' and return them into an array, then
//reverse the order of the array and then returns
//an array with the length of each element.